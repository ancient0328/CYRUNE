# first_success fixtures

This directory is reserved for `cyr verify first-success` integration fixtures.
The tests generate per-case state roots under temporary directories so that no
runtime evidence is adopted into the repository.
